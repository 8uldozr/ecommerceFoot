{% extends 'base.html.twig' %}

{% block content %}

    <div class="hotSell"><p>Séléction de nos maillots les plus vendus !</p></div>

            <div class="row mt-5">
                {% for product in products %}
                    <div class="col-md-3 ">
                    
                        {% include 'product/single_product.html.twig' %}

                    </div>
                {% endfor %}
            </div>

{% endblock %}