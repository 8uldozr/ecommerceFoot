{% extends 'base.html.twig' %}

{% block content %}

    <h3>Séléction de nos maillots les plus vendus !</h3>

            <div class="row mt-5">
                {% for product in products %}
                    <div class="col-md-3">
                    
                        {% include 'product/single_product.html.twig' %}

                    </div>
                {% endfor %}
            </div>

{% endblock %}