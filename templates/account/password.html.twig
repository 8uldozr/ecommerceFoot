{% extends 'base.html.twig' %}

{% block title %}Shop - Modifier mon mot de passe{% endblock %}

{% block content %}

    <h4>Ici vous pouvez modifier votre mot de passe : </h4><br>

    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}

    {{ form(form) }}

    <div class="account">
        <button class="btn btnColor mt-2"> <a href="{{ path('account') }}">Retourner dans votre espace personnel </a></button>
    </div>
{% endblock %}