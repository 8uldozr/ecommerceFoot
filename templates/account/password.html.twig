{% extends 'base.html.twig' %}

{% block title %}Shop - Modifier mon mot de passe{% endblock %}

{% block content %}

    <h2>Ici vous pouvez modifier votre mot de passe</h2>

    <div class="account">
        <a href="{{ path('account') }}">Retourner dans votre espace -></a>
    </div>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}

    {{ form(form) }}
{% endblock %}