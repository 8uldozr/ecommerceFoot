{% extends 'base.html.twig' %}

{% block content %}

    <h4>Pas encore de compte ? Pour nous rejoindre c'est ici. Ca ne prend qu'une minute.</h4>

    {% if notification %}

        <div class="alert alert-info">{{ notification }}</div>
        <hr>

    {% endif %}

    <div class="container">

    {{ form(form)}}

    </div>

{% endblock %}